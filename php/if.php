<?php 
     $a = 5;     // declare variable $a
     $b = 10;    // declare variable $b 
     $c = '10';  // declare variable $c

     if ( $a < $b) {
     	echo "$a is lesser than $b\n" ;    //print the message
     }

     if  ($b > $a) {
     	echo "$b is grater than $a\n" ;    //print the message
     }

     if  ($b >= $c) {
     	echo "$b is grater or equal to $c\n" ;
     }

      if  ($b <= $c) {
     	echo "$b is lesser or equal to $c\n" ;
     }

      if  ($b == $c) {
     	echo "$b value is equal to $c\n" ;
     }
       if  ($b === $c) {
     	echo "$b value and data type is equal to $c\n" ;
     }

      if  ($b != $c) {
     	echo "$b is not equal to $c\n" ;
     }

     if ($b !== $c){
     	echo "$b is not identical to $c\n";
     }



?>